# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Compiled email templates — generated by `npm run build:emails` from
# /emails/*.tsx. Vercel re-generates them at deploy time before bundling
# the function, so we don't keep the compiled output in version control.
api/_compiled-emails/
